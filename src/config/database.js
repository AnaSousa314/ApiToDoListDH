const config = {
  username: 'root',
  password: '',
  database:'api_to_do_list',
  host: 'localhost',
  dialect: 'mysql',
 /*  define:{
    timestamp: false
  } essa definição é aplicada a todos os models */
}

// console.log(config);

module.exports = config;